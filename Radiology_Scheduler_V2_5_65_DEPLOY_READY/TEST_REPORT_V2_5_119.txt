V2.5.119 AUTOMATIC PHASES + OCTOBER FCFS — TEST REPORT
Date: 2026-09-06

PASS — Python compile
  app.py
  db.py
  scheduler_engine.py
  solver_runner.py

PASS — Engine/API version
  APP expected engine: 2.5.119
  scheduler_engine ENGINE_API_VERSION: 2.5.119

PASS — October FCFS activation
  weekend_fcfs_backup_mode(2026,10) = True
  weekend_fcfs_backup_slots(2026,10) = 16
  October slot dates = 3/4, 10/11, 17/18, 24/25; AM+PM each.

PASS — Live Supabase migration
  v2_5_119_automatic_phases_october_fcfs applied
  v2_5_119b_operator_backup_override applied

PASS — Live October phase at test time
  phase = preferences
  preference open = 2026-09-01 00:00 Europe/Vilnius
  preference close / swap open = 2026-09-14 00:00 Europe/Vilnius
  swap + dublis self-service close = 2026-09-16 00:00 Europe/Vilnius

PASS — Automatic clock worker
  pg_cron job v25119-auto-cycle-5min exists and is active.
  Server RPC gates use exact timestamps independent of cron cadence.

PASS — October dubliai board reset
  Live October claim count = 0 after migration (user confirmed no real October dubliai had been selected).
  Live October catalog count = 16.

PASS — Random fill guard
  auto_fill_weekend_backups_v25119(2026,10) currently returns due=false before Sep 16 close; no premature assignment.

PASS — Current October submission points backfilled
  ŠR #1 16
  KE #2 15
  DU #3 14
  SE #4 13
  GE #5 12
  SP #6 11
  These are earned for NEXT month (November), not used for October.

PASS — Separate dubliai layer
  dublis selector remains independent of normal preference form.
  normal preferences close Sep 14; dublis self-selection remains through Sep 16.
  incomplete dubliai no longer block publishing the normal preliminary SYSTEM.
  after Sep 16 missing dubliai are auto-randomized, then materialized for senior review.

PASS — Senior override
  operator_set_weekend_backup_v25119 deployed.
  SP/ŠR operator can reassign/swap a dublis with mandatory audit reason, independent of resident phase windows.

PASS — Manual stage buttons removed for October+ automatic cycle UI
  new cycle shows clock-derived phase instead of requiring Open/Close buttons.
  legacy months keep legacy controls for reproducibility.

PASS — Email removed as operational dependency
  EMAIL_LIFECYCLE_ENABLED=False.
  SMTP/missing-email no longer blocks FINAL or manual correction in the new cycle.

LIMITATION / DESIGN NOTE
  The phase clock is fully automatic server-side, including resident write gates and Sep-16 random dublis fill.
  The Python optimization solver itself cannot be executed by PostgreSQL cron. A valid schedule draft must still be generated by the app/operator. Once generated during an active swap/review phase, activation/freeze is automatic and does not require a separate lifecycle button.
